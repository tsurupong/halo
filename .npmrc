# Strict dependency resolution: packages may only import declared dependencies.
hoist=false
shamefully-hoist=false
strict-peer-dependencies=false
# Link workspace packages via the workspace protocol when versions match.
link-workspace-packages=true
prefer-workspace-packages=true
# WSL2 note: keep the store on the ext4 side (D1 §1.7).
