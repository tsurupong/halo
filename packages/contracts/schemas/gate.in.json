{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://halo.dev/contracts/gate.in.json",
  "title": "gate input",
  "type": "object",
  "properties": {
    "task_id": {
      "type": "string"
    },
    "workdir": {
      "type": "string",
      "description": "Absolute worktree path under inspection."
    },
    "changed_files": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "base": {
      "type": "string",
      "description": "Worktree base commit (the HEAD captured at worktree creation). When present, diff-based gates inspect `git diff <base>` so BOTH committed and uncommitted changes are audited (D4 §4.2); without it, gates fall back to `git diff HEAD` and an executor that commits its work would escape inspection."
    }
  },
  "required": [
    "task_id",
    "workdir",
    "changed_files"
  ],
  "additionalProperties": false,
  "description": "gate input (D1 §1.4)."
}
