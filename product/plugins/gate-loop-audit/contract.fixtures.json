{
  "description": "gate-loop-audit documented I/O (D8 §3.2/§3.4). gate.out emitted on fail only; reason required.",
  "cases": [
    {
      "schema": "gate.in.json",
      "expect": "valid",
      "data": { "task_id": "42", "workdir": "/tmp/wt", "changed_files": ["src/a.ts"] }
    },
    {
      "schema": "gate.in.json",
      "expect": "invalid",
      "data": { "task_id": "42", "workdir": "/tmp/wt" }
    },
    {
      "schema": "gate.out.json",
      "expect": "valid",
      "data": { "reason": "spec_ref not found", "gate": "50-loop-audit" }
    },
    { "schema": "gate.out.json", "expect": "invalid", "data": { "gate": "50-loop-audit" } }
  ]
}
