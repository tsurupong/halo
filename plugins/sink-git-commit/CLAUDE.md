# product/plugins/sink-git-commit/

プラグイン `sink-git-commit` の発見単位。`plugin.json`(entry契約、ADR-0018)と `contract.fixtures.json` のみを置く。実装本体とテストは `product/packages/plugins/src/sink-git-commit/` にある。ここにコードやランチャーを追加しないこと。
