{
  "description": "sink-progress-log documented input (D8 §3.2). Side effects only, no output.",
  "cases": [
    {
      "schema": "sink.in.json",
      "expect": "valid",
      "data": { "task_id": "42", "workdir": "/tmp/wt", "summary": "planned 3 steps" }
    },
    {
      "schema": "sink.in.json",
      "expect": "invalid",
      "data": { "task_id": "42", "workdir": "/tmp/wt" }
    }
  ]
}
