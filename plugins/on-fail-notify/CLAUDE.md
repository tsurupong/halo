# product/plugins/on-fail-notify/

プラグイン `on-fail-notify` の発見単位。`plugin.json`(entry契約、ADR-0018)と `contract.fixtures.json` のみを置く。実装本体とテストは `product/packages/plugins/src/on-fail-notify/` にある。ここにコードやランチャーを追加しないこと。
