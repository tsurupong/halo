# product/plugins/trigger-polling/

プラグイン `trigger-polling` の発見単位。`plugin.json`(entry契約、ADR-0018)と `contract.fixtures.json` のみを置く。実装本体とテストは `product/packages/plugins/src/trigger-polling/` にある。ここにコードやランチャーを追加しないこと。
